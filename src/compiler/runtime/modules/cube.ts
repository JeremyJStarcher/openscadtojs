import { RunTime } from "../../cc/run-time";
import * as TokenType from "../../runtime/token-type";

export default function cube(runtime: RunTime, ...args: TokenType.Value2[]) {
    // Just a place holder
}
